name: Custom app Docker Image
on: [push]
jobs:
  hello-docker:
    runs-on: ubuntu-latest
    steps:
    - name: Hello
      run: echo "Hello, it'll be a great project."
    - name: Repo
      uses: actions/checkout@v3
    - name: Install Dep
      run: |
        make install